type Query {
    users(id: Int): [User]!
}
type Mutation {
    user(name: String!) : User!
}

type User {
    id: ID!
    name: String!
    clockActions: [ClockAction]
}

type ClockAction {
    id: ID!
    type: Int!
    desc: String
}